[package]
name = "async-rust"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest = { version = "0.12", features = ["json"] } # Current stable
tokio = { version = "1", features = ["full"] }
anyhow = "1.0"